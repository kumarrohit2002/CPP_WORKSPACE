#include <stdio.h>
    int main()
    {
        int x = 0;
        if (x == 0)
            printf("true, ");
        else if (x = 10)
            printf("false, ");
        printf("%d\n", x);
        return 0;
    }

