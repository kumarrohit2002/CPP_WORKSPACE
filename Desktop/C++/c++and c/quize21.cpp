#include <stdio.h>
    int main()
    {
        int ch;
        scanf("%d",&ch);
        switch (ch)
        {
           case 1:
              printf("Case A ");
           default:
              printf("Default");
        }
       return 0;
    }
